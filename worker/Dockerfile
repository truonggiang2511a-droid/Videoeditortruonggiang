FROM node:24-bookworm-slim

WORKDIR /app

RUN apt-get update \
  && apt-get install -y --no-install-recommends fonts-dejavu-core \
  && rm -rf /var/lib/apt/lists/*

COPY package.json ./
RUN npm install --omit=dev
COPY server.mjs ./
COPY remotion-renderer.mjs ./

ENV NODE_ENV=production
EXPOSE 8080
CMD ["node", "server.mjs"]
